import { CardShell } from '../components/CardShell'

export function AlertsPage() {
  return (
    <div className="page-grid">
      <CardShell title="Alerts Center">
        <p className="muted">Route-specific page for alert rules, delivery channels, and incident notifications.</p>
      </CardShell>
    </div>
  )
}
